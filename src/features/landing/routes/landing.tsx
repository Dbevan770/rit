import { RoomTabs } from "@/features/room-tabs/components";

export const Landing = () => {
  return (
    <main className="h-full flex items-center justify-center">
      <RoomTabs />
    </main>
  );
};
